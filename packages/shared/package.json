{
  "name": "@claustrum/shared",
  "version": "0.1.1",
  "private": true,
  "type": "module",
  "main": "dist/index.js",
  "types": "src/index.ts",
  "scripts": {
    "build": "tsc -p tsconfig.json && node scripts/copy-outbound-templates.mjs",
    "dev": "tsc -w -p tsconfig.json",
    "test": "node -e \"console.log('shared ok')\"",
    "lint": "tsc -p tsconfig.json --noEmit"
  },
  "dependencies": {
    "zod": "^3.25.76"
  },
  "devDependencies": {
    "typescript": "^5.3.3"
  }
}
